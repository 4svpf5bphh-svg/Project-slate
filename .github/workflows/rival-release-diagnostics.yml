name: one-shot rival-release diagnostics
on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  inspect:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect rival release strategy
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          s=Path("index.html").read_text(encoding="utf-8")
          terms=[
            "releaseWeek","scheduled","stage=cinema","cinemaWeek",
            "aiStartProjects","advanceAI","releaseAIFilm","aiWeeklyFinance",
            "validReleaseWeeks","release calendar","counter","competition",
            "weeklyPlan","releaseProfile","boxOffice"
          ]
          for term in terms:
              print(f"\n===== {term} =====")
              hits=list(re.finditer(re.escape(term),s,re.I))
              print("count",len(hits))
              for m in hits[:40]:
                  print("\n---\n"+s[max(0,m.start()-2200):m.start()+8000])

          print("\n===== FUNCTIONS =====")
          for m in re.finditer(r"function\s+([A-Za-z0-9_]+)\s*\(",s):
              name=m.group(1)
              if re.search(r"(ai|rival|release|cinema|box|schedule|marketing|competition|calendar)",name,re.I):
                  i=m.start();brace=s.find("{",i);depth=0;quote=None;esc=False;j=brace
                  while j<len(s):
                      c=s[j]
                      if quote:
                          if esc: esc=False
                          elif c=="\\": esc=True
                          elif c==quote: quote=None
                      else:
                          if c in ("'", '"', "`"): quote=c
                          elif c=="{": depth+=1
                          elif c=="}":
                              depth-=1
                              if depth==0:
                                  j+=1;break
                      j+=1
                  print(f"\n=== {name} ===\n"+s[i:j])
          PY
